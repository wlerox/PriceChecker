# Üretim build: harici API kökü (protokol + host, path yok). Boş bırakırsan geliştirmede /api Vite proxy kullanılır.
# Firebase Hosting + Cloud Run rewrite kullanıyorsan VITE_API_BASE verme (göreli /api).
# Harici API (Railway vb.): VITE_API_BASE=https://your-api.up.railway.app
